// Write an arrow function that takes a name as a parameter and returns a greeting message, Also please write it using IIFE.
const greeting = ((name) => `Hello, ${name}!`)("Alice");

console.log(greeting);